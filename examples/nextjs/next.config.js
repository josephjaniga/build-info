/** @type {import('next').NextConfig} */
const nextConfig = {
  // This will help us see the bundling issue clearly
  // We'll add webpack configuration here later if needed
};

module.exports = nextConfig;
